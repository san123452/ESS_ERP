<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- namespace는 반드시 ItemMapper 인터페이스의 경로와 똑같아야 연결됩니다! -->
<mapper namespace="com.ess.erp.mapper.ItemMapper">

    <!-- 1. 품목 등록 쿼리 -->
    <!-- #{변수명}은 DTO의 필드명과 일치해야 값이 쏙 들어갑니다. -->
    <insert id="insertItem" parameterType="com.ess.erp.domain.ItemDTO">
        INSERT INTO TB_ITEM (
            ITEM_CD, ITEM_NM, ITEM_TYPE, UNIT, PRICE, STOCK_QTY, SAFE_QTY, WH_LOCATION, LOT_USE_YN, USE_YN, REG_DT, MOD_DT
        ) VALUES (
            #{itemCd}, #{itemNm}, #{itemType}, #{unit}, #{price}, 0, #{safeQty}, #{whLocation}, 'N', 'Y', NOW(), NOW()
        )
    </insert>

    <!-- 2. 품목 목록 조회 쿼리 -->
    <select id="selectItemList" resultType="com.ess.erp.domain.ItemDTO">
        SELECT 
            ITEM_CD as itemCd, ITEM_NM as itemNm, ITEM_TYPE as itemType, UNIT as unit, PRICE as price, STOCK_QTY as stockQty, SAFE_QTY as safeQty, WH_LOCATION as whLocation, USE_YN as useYn
        FROM TB_ITEM
        WHERE USE_YN = 'Y'
        ORDER BY ITEM_CD ASC
    </select>

</mapper>