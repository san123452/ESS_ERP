<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ess.erp.mapper.CommonItemMapper">

    <select id="selectCurrentStock" resultType="int">
        SELECT STOCK_QTY FROM TB_ITEM WHERE ITEM_CD = #{itemCd}
    </select>

    <update id="updateItemStock">
        UPDATE TB_ITEM 
        SET STOCK_QTY = STOCK_QTY + #{qty}, MOD_DT = NOW()
        WHERE ITEM_CD = #{itemCd}
    </update>

    <!-- 수불 이력은 물리 삭제 및 수정 절대 금지 (블랙박스) -->
    <insert id="insertItemLog">
        INSERT INTO TB_ITEM_LOG (
            ITEM_CD, INOUT_TYPE, QTY, BEFORE_QTY, AFTER_QTY, REF_NO, EMP_ID, REG_DT
        ) VALUES (
            #{itemCd}, #{inoutType}, #{qty}, #{beforeQty}, #{afterQty}, #{refNo}, #{empId}, NOW()
        )
    </insert>

</mapper>
